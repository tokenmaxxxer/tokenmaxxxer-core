# directive.sh shape classification (issue-78/#173/#175/#177) used to
# live here as a regex table, consumed at runtime by
# gate_is_role_directive_stub (core/hooks/lib/gate-lib.sh). Removed by
# issue-180: three rounds of per-repo pattern rows (#78, #173, #175,
# #177) each still left real Batch-1 fleet-scan repos scanning dirty
# (docs/issue-180/reports/implementation/survey.md). Replaced with a
# structural per-line classifier built directly into
# gate_is_role_directive_stub — see that function's comment. Nothing
# else in this repo reads this file; it is kept only so a future
# canon-drift mechanism has an obvious, already-registered home.
