# Proposal: coding (issue-21)

Perform a literal prose sweep replacing the five stale `muster` mentions identified in the survey
with `on-the-record`, in `README.md`, `freelunch/README.md`, `scout/README.md`,
`core/hooks/directive.sh`, and `core/hooks/board-gate.sh`. This is a small, low-risk, fully-specified
text fix with no behavioral or structural change, so phase 1 and phase 2 are being delivered together
in a single PR rather than as separate review rounds.
