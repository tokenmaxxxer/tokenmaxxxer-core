---
issue: 323
role: execution-observation
author: execution-observation
loop_state: landed
upstream:
  - path: issue-323/implementation (PR #324)
    sha: 8fb4857a64e84b9d746a61a4b15dd29ab90924c8
subject: PR #324 — warrant/hooks/scope-gate.sh's embedded payload heredoc replaced with a real sibling file (warrant/hooks/lib/scope-gate.py), plus a disclosed audit of the other 21-25 heredoc-using hook scripts
test: core/hooks/tests/run-scope-gate-tests.sh, core/hooks/tests/run-role-gates-tests.sh (issue's own named regression suites); cross-checked with an independent live ENOSPC reproduction (before/after) built from scratch in this session, not a re-run of the PR's pasted transcripts
result: passed
assertedBy: execution-observation (independently re-derived from source on issue-323/implementation @ 8fb4857, plus a hand-built unshare/tmpfs reproduction harness and a from-scratch repo-wide heredoc grep, both run in this session against that same commit)
---

# issue-323 — execution-observation record

## What was done

Independently verified PR #324
(`issue-323: fix scope-gate.sh heredoc ENOSPC exposure, audit remaining
21+ hook scripts`, head `8fb4857a64e84b9d746a61a4b15dd29ab90924c8`) against
issue #323's four Acceptance checks. Nothing below is taken on the PR's or
its own `docs/issue-323/reports/implementation.md`'s word — each item was
re-derived or re-executed live against that commit in a fresh worktree
(`git worktree add /tmp/verify323/impl-checkout 8fb4857`).

**1. Source-level fix, confirmed by reading the actual diff, not the PR's narration.**

`git diff cd02e72 8fb4857 --stat` shows exactly the claimed shape: `warrant/hooks/scope-gate.sh` shrinks from 504 to 8 lines of payload-dispatch code (504 lines removed, all of it the old `python3 <<'PY' ... PY` heredoc body), and a new file `warrant/hooks/lib/scope-gate.py` (501 lines) appears. The post-fix `scope-gate.sh` (read in full) contains no heredoc anywhere; its last non-comment lines are:

```
SCOPE_GATE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
WARRANT_PAYLOAD="$payload" python3 "$SCOPE_GATE_DIR/lib/scope-gate.py"
exit $?
```

**2. Extracted payload is byte-identical to the pre-fix heredoc body, modulo the stated header comment.**

Extracted the pre-fix heredoc body verbatim from `f30c912:warrant/hooks/scope-gate.sh` (`sed` between the `<<'PY'` line and the closing `PY`, 494 lines) and diffed it against `8fb4857:warrant/hooks/lib/scope-gate.py` (501 lines):

```
$ diff /tmp/verify323/pre_heredoc_body.py /tmp/verify323/post_lib.py
0a1,7
> # scope-gate.sh's own PreToolUse payload logic, as a real file rather than a
> # bash heredoc (issue-323): a heredoc's body is spooled to a temp file under
> # $TMPDIR before the interpreter ever runs, so under disk/inode exhaustion the
> # heredoc creation itself fails -- indistinguishable from this gate's own
> # fail-closed refusal. Loading this file directly has no such temp file to
> # create.
>
```

The only difference is the claimed 7-line header comment (494 + 7 = 501). No logic changed.

**3. Live ENOSPC reproduction, before and after, built independently rather than re-run from the PR's transcript.**

Built a fresh harness (`/tmp/verify323/run_repro.sh`) rather than reusing the implementation record's own scripts: mounts a 0-free-inode tmpfs (`mount -t tmpfs -o size=16k,nr_inodes=2`, one filler file consuming the spare inode) inside an unprivileged `unshare --user --map-root-user --mount` namespace, points `TMPDIR` at it, and runs a target scope-gate script against a minimal JSON payload on stdin.

Before (a from-scratch padded copy of the pre-fix heredoc script — pre-fix `scope-gate.sh`'s heredoc body padded past bash's pipe-buffer threshold with inert `# pad` comment lines, generated by a standalone Python script, independent of the implementation record's own padding artifact):

```
$ unshare --user --map-root-user --mount /tmp/verify323/run_repro.sh \
    /tmp/verify323/scope-gate-before-padded.sh /tmp/verify323/payload.json
파일 시스템    Inodes IUsed IFree IUse% 마운트위치
tmpfs               2     2     0  100% /tmp/verify323/mnt
/tmp/verify323/scope-gate-before-padded.sh: 줄 29: here-document를 위한 임시 파일을 생성할 수 없음: 장치에 남은 공간이 없음
fail-closed: gate aborted (rc=1)
outer-exit=2
```

Byte-for-byte the same confusing failure the issue reports and `gate_trap_fail_closed`'s message in `core/hooks/lib/gate-lib.sh:43` (`echo "fail-closed: gate aborted (rc=$rc)" >&2`) — confirmed to be the exact source of that second line by reading the function directly.

After (the real, unmodified fixed `warrant/hooks/scope-gate.sh` from the `8fb4857` worktree, same exhausted-tmpfs namespace, same payload):

```
$ unshare --user --map-root-user --mount /tmp/verify323/run_repro.sh \
    /tmp/verify323/impl-checkout/warrant/hooks/scope-gate.sh /tmp/verify323/payload.json
파일 시스템    Inodes IUsed IFree IUse% 마운트위치
tmpfs               2     2     0  100% /tmp/verify323/mnt
outer-exit=0
```

No ENOSPC error, no fail-closed abort — the fixed script runs to completion (this repo checkout has no `docs/proposals/` directory, so the Python payload's own logic stands down and allows; the point being verified here is the shell layer, which never touches `$TMPDIR` at all now, independent of what the Python payload decides). This matches acceptance option (a) — the fix removes the exposure rather than merely relabeling it — and is not a re-assertion of the implementation record's own before/after transcript; it is a second, independently-constructed one landing on the same conclusion. Did not additionally reproduce the deny-branch case under exhaustion (the implementation record's own transcript does show it): the shell-level fix being verified here is content-independent of the Python payload's decision, since the heredoc removal means no `$TMPDIR` interaction happens before python3 starts regardless of which branch the payload logic takes, so the allow-branch demonstration above is sufficient to confirm the mechanism-level fix.

**4. Regression suites, re-run directly against `8fb4857`, not the pasted PR counts.**

```
$ bash core/hooks/tests/run-scope-gate-tests.sh 2>&1 | tail -5
...
== 46 passed, 0 failed ==

$ bash core/hooks/tests/run-role-gates-tests.sh 2>&1 | tail -5
...
role-gates: 83 passed, 0 failed
```

Matches the PR's claimed 46/46 and 83/83 exactly — re-executed, not copied.

**5. The 21-25-script audit, cross-checked with an independent grep, not trusted from the implementation record's list.**

```
$ grep -rlE '<<-?[[:space:]]*['"'"'"]?[A-Za-z_]+|<<<' --include='*.sh' . | grep -v '/tests/'
```

returned 26 files. One (`warrant/hooks/scope-gate.sh`) is a false positive — line 29 is a *comment* referencing the old heredoc syntax (`# issue-323: ... a python3 <<'PY' ... PY heredoc.`), not an actual heredoc; confirmed by reading the line. Excluding it leaves exactly 25 files, and the file list matches the implementation record's "25 other non-test scripts" enumeration name-for-name (all three of its groups: the 11 `python3 <<'PY'` scripts, the 3 `IFS read <<'PY'` scripts, and the 9 `cat <<EOF` scripts, plus `hunt-tier.sh`'s `done <<EOF2` and `gate-lib.sh`'s `done <<< "$other"` here-string). No discrepancy found beyond the one already disclosed in the record (this session's wider net vs. the issue's own "22" grep count).

**6. PR body / issue trailer check.**

PR #324's body carries `Closes #323` and all three test-plan checkboxes are checked; the commit itself carries `Subject: issue-323` (the phase-2 delivery trailer convention — `Closes` lives in the PR body per this repo's own convention, not the commit trailer, consistent with the phase-1/phase-2 PR-trailer split this role observes elsewhere). The PR is still open (not yet merged to `main`) as of this observation; this record's basis is `8fb4857`, which matches the commit named in this record's own upstream frontmatter.

## Why

Verification re-derived each of PR #324's four central claims from source and from independently-built reproductions rather than reading and restating the implementation record's own pasted transcripts, per this role's purpose (an execution-observation record is only worth writing if it can fail — trusting the subject's own claims would make it a formality). All four came back confirmed:

1. The heredoc is gone; the shell script now loads a real file.
2. The extracted Python logic is unchanged (byte-identical except a disclosed header comment).
3. The ENOSPC failure reproduces before the fix and is absent after it, on a from-scratch harness independent of the implementation record's own scripts.
4. The named regression suites pass unchanged, re-run directly.
5. The 21-25-script audit is accurate against an independent grep.

No discrepancy was found between the PR's claims and what re-executing/re-deriving them actually shows.

## Upstream basis

- `issue-323/implementation` @ `8fb4857a64e84b9d746a61a4b15dd29ab90924c8` (PR #324) — `warrant/hooks/scope-gate.sh`, `warrant/hooks/lib/scope-gate.py`, `docs/issue-323/reports/implementation.md`.
- `f30c9120df4014602a119d7debe1f0e44f5bdb23` — pre-fix `warrant/hooks/scope-gate.sh`, used as the basis for this record's own independent padded-heredoc reproduction and the byte-identity diff.
- `core/hooks/lib/gate-lib.sh` @ `8fb4857` — `gate_trap_fail_closed` (line 43), confirmed as the literal source of the `fail-closed: gate aborted (rc=$rc)` message.
- `core/hooks/tests/run-scope-gate-tests.sh`, `core/hooks/tests/run-role-gates-tests.sh` @ `8fb4857` — re-executed directly, not read.
- Issue #323 (`gh issue view 323`) — verbatim acceptance criteria.
- This record's own reproduction artifacts (`/tmp/verify323/*`), not committed to the repo — a scratch harness, not a project deliverable.

## Open findings

None. All four acceptance checks independently confirmed:

1. **ENOSPC-during-heredoc live reproduction** — confirmed, via a from-scratch harness independent of the implementation record's own.
2. **Post-fix behavior under the same constrained TMPDIR** — confirmed option (a): the fixed gate runs to completion with no ENOSPC-related output at all, because the heredoc's temp-file dependency is removed entirely rather than relabeled.
3. **Normal-condition regression check** — confirmed unchanged: `run-scope-gate-tests.sh` 46/46, `run-role-gates-tests.sh` 83/83, re-run directly.
4. **Disposition of the other 21+ heredoc-using scripts** — confirmed accurate: an independent repo-wide grep returns the same 25-script list (after excluding one confirmed false positive, a comment referencing the old heredoc), each already carrying an explicit "not fixed in this pass, deferred to a follow-up issue" disposition in the implementation record. Filing that follow-up issue is out of scope for both the implementation role and this observation, per the role-handoff contract's invariants (this role does not self-file issues).

`observability-phase-trace` (the cross-family skill mapped onto this session) was not invoked, per this record's own phase-1 proposal: issue #323 is a bash-heredoc/ENOSPC bugfix with no observability-methodology phase-1 artifact (RED/USE-style panel work) to trace against — stated explicitly here rather than silently dropped.

## Next steps

None — `loop_state: landed`. This record's own scope (verifying PR #324 against issue #323's four acceptance checks) is complete with a `passed` verdict. Any follow-up (filing the issue for the other 25 heredoc-using scripts, prioritizing `core/hooks/board-gate.sh` as the closest to the ~64KB threshold) is left to the user/maintainers, as already stated in the implementation record and reaffirmed here.
