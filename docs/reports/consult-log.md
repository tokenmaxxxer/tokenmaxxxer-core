- 2026-08-15T15:27:49.045781+00:00 | role=secure-coding | verb=consult | issue=none | question='core#218: warrant scope-gate read-only vouch. Proposed change: allow single-pipe pipelines when every segment matches the read-only allowlist; reject ; & backtick $() || > < and newline; exclude <> fr' | outcome="ok: 구조(단일 파이프, 세그먼트별 allowlist, 문자 denylist)는 맞는 방향이지만 그 자체로 no-unvetted-execution을 보장하지 않는다. 실행 경로가 shell=True인지 argv 배열인지가 먼저 확정돼야 하고, xargs/find -exec/sed -i/awk system()/less !사용 같은 'read 명령이지만 exec·w"
